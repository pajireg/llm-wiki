# llm-wiki Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Karpathy의 "LLM이 유지하는 위키" 패턴을 Claude Code 플러그인(`llm-wiki`)으로 구현하고, 사용자의 통합 지식 vault(`~/Vaults/wiki/`)를 그 플러그인으로 부트스트랩한다.

**Architecture:** 두 git 레포 분리. public 플러그인 레포 = 명령·스킬·훅·schema 템플릿. private vault 레포 = 콘텐츠. 플러그인이 `/wiki-init`으로 vault를 부트스트랩하고, 이후 `/wiki-ingest`/`/wiki-ask`/`/wiki-lint`로 vault를 작동시킨다.

**Tech Stack:** Claude Code Plugin (`.claude-plugin/plugin.json`), 슬래시 명령(마크다운), SessionEnd hook (bash + 인라인 Python3), Markdown + YAML frontmatter, bats (셸 단위 테스트), Python 3 (schema 유효성 검증 스크립트, 표준 라이브러리만 사용).

**Spec:** [`docs/superpowers/specs/2026-05-18-llm-wiki-design.md`](../specs/2026-05-18-llm-wiki-design.md)

**Pre-flight 결정사항:**
- 플러그인 레포 경로: `~/Projects/llm-wiki/` (사용자 합의 시 시작). 다른 경로 원하면 plan 전체에서 `<PLUGIN_REPO>` 치환.
- 사용자 vault 경로: `~/Vaults/wiki/` (확정).
- 모든 step은 플러그인 레포 cwd 기준. 다를 경우 명시.

---

## File Structure

### 플러그인 레포 `~/Projects/llm-wiki/`

| 파일 | 책임 |
|---|---|
| `.claude-plugin/plugin.json` | 플러그인 메타데이터 |
| `commands/wiki-init.md` | vault 부트스트랩 명령 |
| `commands/wiki-ingest.md` | source → wiki 합성 명령 |
| `commands/wiki-ask.md` | wiki 컨텍스트 기반 질의응답 명령 |
| `commands/wiki-lint.md` | 8항목 위키 점검 명령 |
| `commands/wiki-upgrade-schema.md` | schema 템플릿 diff/머지 명령 |
| `skills/llm-wiki/SKILL.md` | 위키 운영 원칙·체크리스트 (모든 명령이 로드) |
| `hooks/session-end-capture.sh` | SessionEnd 훅: transcript → sources/claude-sessions/ |
| `templates/schema/README.md` | vault 헌법 (LLM이 매번 로드) |
| `templates/schema/page-types.md` | 5종 페이지 타입 정의 |
| `templates/schema/relations.md` | 5종 관계 타입 정의 |
| `templates/schema/namespaces.md` | namespace 정책 + cwd 매핑 (빈 템플릿) |
| `templates/schema/ingest-rules.md` | ingest 절차 |
| `templates/schema/lint-rules.md` | 린팅 8항목 체크리스트 |
| `templates/README.template.md` | vault용 README 템플릿 |
| `templates/gitignore.template` | vault용 .gitignore |
| `scripts/validate-schema.py` | schema 템플릿 자체 유효성 검증 |
| `scripts/install-hook.sh` | 글로벌 SessionEnd 훅 등록 안내 |
| `tests/test_session_end_capture.bats` | 훅 단위 테스트 |
| `tests/test_validate_schema.py` | 검증 스크립트 단위 테스트 |
| `tests/fixtures/sample-source.md` | 통합 테스트용 픽스처 |
| `README.md` | 플러그인 영문 README |
| `LICENSE` | MIT |
| `.gitignore` | 플러그인 레포용 |

### Vault `~/Vaults/wiki/` (Task 9에서 부트스트랩됨)

플러그인의 `/wiki-init`이 생성. spec §3 vault 구조 그대로.

---

## Task 1: 플러그인 레포 골격

**Files:**
- Create: `~/Projects/llm-wiki/.claude-plugin/plugin.json`
- Create: `~/Projects/llm-wiki/README.md`
- Create: `~/Projects/llm-wiki/LICENSE`
- Create: `~/Projects/llm-wiki/.gitignore`

- [ ] **Step 1: 사용자와 플러그인 레포 경로 확정**

사용자에게 묻기: "플러그인 레포를 어디에 만들까요? 기본 제안은 `~/Projects/llm-wiki/`입니다." 확정된 경로를 이후 모든 step에서 사용. 본 문서에선 `~/Projects/llm-wiki/`로 표기.

- [ ] **Step 2: 디렉터리 생성 및 git init**

```bash
mkdir -p ~/Projects/llm-wiki
cd ~/Projects/llm-wiki
git init
mkdir -p .claude-plugin commands skills/llm-wiki hooks templates/schema scripts tests/fixtures
```

- [ ] **Step 3: `.claude-plugin/plugin.json` 작성**

```json
{
  "name": "llm-wiki",
  "version": "0.1.0",
  "description": "Memex realized with LLMs — a wiki that the model maintains.",
  "author": "<your-name>",
  "homepage": "https://github.com/<you>/llm-wiki",
  "license": "MIT"
}
```

- [ ] **Step 4: `.gitignore` 작성**

```
.DS_Store
*.swp
__pycache__/
*.pyc
.pytest_cache/
node_modules/
```

- [ ] **Step 5: `LICENSE` 작성 (MIT)**

표준 MIT 라이선스 텍스트, 연도 2026, 저작자 사용자 이름.

- [ ] **Step 6: `README.md` 초안 작성**

```markdown
# llm-wiki

> Memex realized with LLMs.

A Claude Code plugin that turns your knowledge into a wiki the model maintains.
Inspired by [Karpathy's LLM wiki pattern](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f).

## Install

\`\`\`
/plugin install <repo-url>
\`\`\`

## Bootstrap your vault

\`\`\`bash
mkdir -p ~/Vaults/<your-wiki>
cd ~/Vaults/<your-wiki>
claude
> /wiki-init
\`\`\`

See `commands/` for available commands.

## License

MIT
```

(상세 사용법은 Task 9 이후 보강.)

- [ ] **Step 7: 검증 - 디렉터리 구조 확인**

```bash
cd ~/Projects/llm-wiki
ls -la
find . -type d | head -20
```

기대 출력:
- `.claude-plugin/`, `commands/`, `skills/llm-wiki/`, `hooks/`, `templates/schema/`, `scripts/`, `tests/fixtures/` 모두 존재
- `plugin.json`, `LICENSE`, `README.md`, `.gitignore` 존재

- [ ] **Step 8: Commit**

```bash
cd ~/Projects/llm-wiki
git add .
git commit -m "feat: plugin repo skeleton with plugin.json, README, LICENSE"
```

---

## Task 2: Schema 템플릿 + 유효성 검증

Schema는 모든 명령이 매번 컨텍스트에 로드하는 시스템의 "헌법". 6개 파일이 spec §4 데이터 모델 전체를 표현한다. 또한 validate-schema.py로 frontmatter 표기 일관성을 보장.

**Files:**
- Create: `~/Projects/llm-wiki/templates/schema/README.md`
- Create: `~/Projects/llm-wiki/templates/schema/page-types.md`
- Create: `~/Projects/llm-wiki/templates/schema/relations.md`
- Create: `~/Projects/llm-wiki/templates/schema/namespaces.md`
- Create: `~/Projects/llm-wiki/templates/schema/ingest-rules.md`
- Create: `~/Projects/llm-wiki/templates/schema/lint-rules.md`
- Create: `~/Projects/llm-wiki/scripts/validate-schema.py`
- Create: `~/Projects/llm-wiki/tests/test_validate_schema.py`

- [ ] **Step 1: 실패하는 검증 테스트 먼저 작성**

`tests/test_validate_schema.py`:

```python
import subprocess
import sys
from pathlib import Path

PLUGIN_ROOT = Path(__file__).resolve().parents[1]
VALIDATE = PLUGIN_ROOT / "scripts" / "validate-schema.py"
TEMPLATES = PLUGIN_ROOT / "templates" / "schema"

def test_validate_schema_exits_zero_for_template():
    result = subprocess.run(
        [sys.executable, str(VALIDATE), str(TEMPLATES)],
        capture_output=True, text=True,
    )
    assert result.returncode == 0, result.stderr

def test_validate_schema_lists_all_six_files():
    result = subprocess.run(
        [sys.executable, str(VALIDATE), str(TEMPLATES), "--list"],
        capture_output=True, text=True,
    )
    expected = {"README.md", "page-types.md", "relations.md",
                "namespaces.md", "ingest-rules.md", "lint-rules.md"}
    found = set(result.stdout.strip().splitlines())
    assert found == expected, f"missing: {expected - found}"
```

- [ ] **Step 2: 테스트 실행하여 실패 확인**

```bash
cd ~/Projects/llm-wiki
python -m pytest tests/test_validate_schema.py -v
```

기대: FAIL — `validate-schema.py` 존재하지 않음 또는 schema 파일 없음.

- [ ] **Step 3: `templates/schema/README.md` 작성 (위키 헌법)**

```markdown
# Wiki Schema — Constitution

This directory is the authoritative ruleset that the LLM loads on every wiki operation.
You own these files. Edit them to change wiki behavior.

## Three layers (Karpathy)

- `sources/` — immutable originals. Never modify. Correct via new source + `supersedes`.
- `wiki/<namespace>/` — synthesized pages. The LLM maintains these.
- `schema/` — this directory. The rules.

## Hard rules (day-1 strict)

1. Every wiki page MUST have a non-empty `sources:` frontmatter array.
2. Every page MUST have `type:` (one of 5) and `namespace:` (defined in namespaces.md).
3. Sources are read-only. New facts → new source + relation update.
4. Frontmatter is the source of truth. Body wikilinks are mentions; frontmatter relations are semantics.
5. LLM never auto-edits this `schema/` directory.

## How LLM uses this directory

On every `/wiki-*` command, load all files in this directory into context BEFORE acting.

## See also

- `page-types.md` — 5 page types
- `relations.md` — 5 relation types
- `namespaces.md` — domain isolation + cwd mapping
- `ingest-rules.md` — ingest procedure
- `lint-rules.md` — 8 lint checks
```

- [ ] **Step 4: `templates/schema/page-types.md` 작성**

```markdown
# Page Types

5 types. Every page is exactly one. Ingest MUST classify.

## `topic`
Evolving concept page. The wiki's main product.
- Filename: `<slug>.md` (Korean slug allowed)
- Frontmatter additions:
  - `aliases: [...]` — duplicate prevention
  - `related: ["[[other-topic]]"]`
  - `contradicts: ["[[other-topic]]"]`
  - `part_of: ["[[parent-topic]]"]`
  - `supersedes: ["[[old-topic]]"]`

## `entity`
Person, tool, company, project. Stable.
- Filename: `<name-slug>.md`
- Additions:
  - `kind: person | tool | company | project | other`
  - `aliases: [...]`
  - `related: [...]`

## `note`
Raw observation/insight. Pre-synthesis.
- Filename: `<YYYY-MM-DD>-<slug>.md`
- Additions:
  - `topics: ["[[topic-this-belongs-to]]"]`
  - `related: [...]`
  - `confidence: high | medium | low`

## `source`
Original meta (in `sources/`). Read-only.
- Filename: `<YYYY-MM-DD>-<slug>.md`
- Additions:
  - `source_type: claude_session | article | book | conversation | other`
  - `ingested_at: <ISO datetime>`
  - `url: "https://..."` (if applicable)
  - `processed: false` (true after /wiki-ingest)
  - `session_id`, `cwd` (claude_session only)

## `question`
Unanswered, follow-up target.
- Filename: `q-<slug>.md`
- Additions:
  - `topics: ["[[related-topic]]"]`
  - `status: open | investigating | answered | abandoned`
  - `answered_by: ["[[topic-or-note]]"]` (status=answered)

## Common frontmatter (all types)

\`\`\`yaml
---
type: topic | entity | note | source | question
namespace: personal | work | tech | projects | people
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources:
  - "[[<source-page>]]"   # wiki pages: at least one
tags: []
---
\`\`\`

If unsure on classification, use the nearest type and set `confidence: low` (note) or note in body.
```

- [ ] **Step 5: `templates/schema/relations.md` 작성**

```markdown
# Relations

5 typed relations. Use frontmatter fields, not just body wikilinks.

| Relation | Meaning | Direction |
|---|---|---|
| `related` | Weak association | Bidirectional (recommended) |
| `part_of` | Hierarchy (A is in B) | Directional (A → B) |
| `contradicts` | Conflict. Lint flags. | Bidirectional |
| `supersedes` | A replaces B. B preserved with marker. | Directional (A → B) |
| `derived_from` | wiki page → source (replaceable by `sources:`) | Directional |

## Body wikilinks vs frontmatter relations

- Body `[[X]]` = "this page mentions X"
- Frontmatter `related: ["[[X]]"]` = "this page is semantically related to X"

Significant connections MUST be in frontmatter. Mentions alone in body are OK for incidental refs.

## Bidirectional integrity

For `related` and `contradicts`, when adding A → B, also add B → A. Lint checks this.
For directional (`part_of`, `supersedes`, `derived_from`), only the source side declares.
```

- [ ] **Step 6: `templates/schema/namespaces.md` 작성**

```markdown
# Namespaces

5 namespaces. Every page has exactly one in frontmatter.

| Namespace | Use for |
|---|---|
| `personal` | Personal knowledge, goals, habits |
| `work` | Work projects, decisions, meetings |
| `tech` | Tech learning: languages, tools, patterns |
| `projects` | Active projects (personal or work) |
| `people` | Person entities (cross-cutting) |

## Link policy

\`\`\`yaml
namespaces:
  personal: { allow_links_to: [all] }
  work:     { allow_links_to: [all] }
  tech:     { allow_links_to: [all] }
  projects: { allow_links_to: [all] }
  people:   { allow_links_to: [all] }
\`\`\`

If you have stricter requirements (e.g., work isolation), edit the `allow_links_to` arrays.

## Source-to-namespace mapping (cwd-based)

When the SessionEnd hook captures a Claude Code session, it determines the source's namespace from the session's working directory using this table.

**Fill in your actual directory structure below.** Patterns are matched in order; first match wins.

\`\`\`yaml
cwd_to_namespace:
  # Example (replace with yours):
  # "/Users/<you>/Vaults/wiki/**": personal
  # "/Users/<you>/projects/**":     tech
  # "/Users/<you>/work/**":         work
  default: personal
\`\`\`

Patterns use shell glob with `**` for recursive match.
```

- [ ] **Step 7: `templates/schema/ingest-rules.md` 작성**

```markdown
# Ingest Rules

Procedure the LLM follows on `/wiki-ingest`.

## Steps (in order)

1. **Load schema** — read all files in `schema/`.
2. **Read source** — read target source file. Note its `namespace`.
3. **Identify related pages** — search wiki:
   - Filename + aliases match
   - frontmatter `topics:` references
   - body text grep
   - Same namespace first, then cross-namespace
4. **Synthesize**:
   - Update existing `topic`/`entity` if the source adds info.
   - Create new `topic` if a coherent new concept emerged.
   - Create `note` if the insight is too raw to synthesize yet.
   - Create `question` if the source raises an unresolved question.
5. **Maintain relations**:
   - All touched pages get `sources: [[<this-source>]]` appended (deduplicated).
   - Significant connections → frontmatter relations, not just body wikilinks.
   - Detected contradiction → add `contradicts:` on both sides.
6. **Mark source** — set `processed: true`, update `updated:`.
7. **Commit** (if vault is git repo) — meaningful message: `wiki: ingest <source-name>`.
8. **Report** — list touched pages with one-line change summary.

## Page creation rules

- Slug from filename. Korean slugs allowed.
- New pages MUST have full common frontmatter + type-specific fields.
- New pages MUST have non-empty `sources:`.

## When not to ingest

- Source has `processed: true` already.
- Source body is empty/trivial (e.g., 2-line session) — skip silently, do not mark `processed`.

## Forbidden

- Editing files under `sources/`.
- Editing files under `schema/`.
- Removing existing `sources:` entries on a page.
```

- [ ] **Step 8: `templates/schema/lint-rules.md` 작성**

```markdown
# Lint Rules

8 checks performed by `/wiki-lint`. Output to `lint-reports/<YYYY-MM-DD>-lint.md`.
Findings only — no auto-fixes.

## Checks

1. **Unprocessed source backlog**
   - Find all `sources/**/*.md` with frontmatter `processed: false`.
   - Report count + oldest 10.

2. **Duplicate candidates** (topics/entities)
   - For each pair of topics in same namespace:
     - Exact-title or alias-overlap → high confidence.
     - Body cosine-similarity > 0.7 (TF-IDF over body) → low confidence.
   - Report pairs.

3. **Orphan pages**
   - Pages with zero inbound references (no other page wikilinks to it AND no frontmatter relations point to it).
   - Sources are not subject to this check.

4. **Broken wikilinks**
   - `[[X]]` references to a page that doesn't exist.
   - Both body and frontmatter relations checked.

5. **Contradiction markers**
   - All pages with non-empty `contradicts:`.
   - Lists pairs for user decision.

6. **Stale open questions**
   - `question` pages with `status: open` and `updated:` older than 30 days.

7. **Frontmatter violations**
   - Missing required fields (`type`, `namespace`, `created`, `updated`).
   - Invalid `type` (not in 5).
   - Invalid `namespace` (not in 5).
   - Invalid type-specific fields per `page-types.md`.

8. **Missing sources**
   - wiki pages (any type except `source`) with empty `sources:`.

## Report format

\`\`\`markdown
# Lint Report YYYY-MM-DD

## Summary
- Unprocessed sources: N
- Duplicate candidates: N pairs
- Orphan pages: N
- Broken wikilinks: N
- Contradictions: N pairs
- Stale questions: N
- Frontmatter violations: N
- Missing sources: N

## Details
<each section with wikilinks to offending pages>
\`\`\`

## What lint does NOT do

- Auto-fix anything.
- Modify any page.
- Decide between duplicate candidates.

User reviews report and issues follow-up commands ("merge X and Y", "fix broken links").
```

- [ ] **Step 9: `scripts/validate-schema.py` 작성**

```python
#!/usr/bin/env python3
"""Validate that a schema/ directory has the 6 required files and basic structure."""
import sys
from pathlib import Path

REQUIRED = {
    "README.md", "page-types.md", "relations.md",
    "namespaces.md", "ingest-rules.md", "lint-rules.md",
}

def main():
    if len(sys.argv) < 2:
        print("usage: validate-schema.py <schema-dir> [--list]", file=sys.stderr)
        return 2

    schema_dir = Path(sys.argv[1])
    list_mode = "--list" in sys.argv[2:]

    if not schema_dir.is_dir():
        print(f"not a directory: {schema_dir}", file=sys.stderr)
        return 1

    found = {p.name for p in schema_dir.iterdir() if p.is_file() and p.suffix == ".md"}

    if list_mode:
        for name in sorted(found):
            print(name)
        return 0

    missing = REQUIRED - found
    if missing:
        for m in sorted(missing):
            print(f"missing: {m}", file=sys.stderr)
        return 1

    # Basic non-empty check
    for name in REQUIRED:
        if (schema_dir / name).stat().st_size == 0:
            print(f"empty: {name}", file=sys.stderr)
            return 1

    return 0

if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 10: 테스트 다시 실행하여 통과 확인**

```bash
cd ~/Projects/llm-wiki
chmod +x scripts/validate-schema.py
python -m pytest tests/test_validate_schema.py -v
```

기대: 2개 테스트 PASS.

- [ ] **Step 11: Commit**

```bash
cd ~/Projects/llm-wiki
git add templates/schema scripts/validate-schema.py tests/test_validate_schema.py
git commit -m "feat: schema templates (6 files) and validator"
```

---

## Task 3: `llm-wiki` 스킬 정의

모든 `/wiki-*` 명령이 첫 단계에서 로드하는 운영 원칙·체크리스트.

**Files:**
- Create: `~/Projects/llm-wiki/skills/llm-wiki/SKILL.md`

- [ ] **Step 1: `skills/llm-wiki/SKILL.md` 작성**

```markdown
---
name: llm-wiki
description: Operating principles and checklists for maintaining an LLM-curated wiki. Loaded by all /wiki-* commands.
---

# llm-wiki: Operating Principles

Use this skill when invoked from `/wiki-init`, `/wiki-ingest`, `/wiki-ask`, `/wiki-lint`, or `/wiki-upgrade-schema`.

## Always load first

Before any wiki action, read these from the current vault:

1. `schema/README.md` — constitution
2. `schema/page-types.md`
3. `schema/relations.md`
4. `schema/namespaces.md`
5. `schema/ingest-rules.md`
6. `schema/lint-rules.md`

If any are missing, abort and tell the user to run `/wiki-init`.

## Hard invariants (NEVER violate)

1. Never modify files under `sources/`.
2. Never modify files under `schema/` unless this is `/wiki-upgrade-schema` and the user explicitly approved a specific diff.
3. Every new wiki page MUST have non-empty `sources:` frontmatter.
4. Every page MUST have valid `type` and `namespace` per schema.
5. When making changes, preserve existing `sources:` and relation entries — append, never replace.

## Commit behavior

If the vault is a git repo (i.e., `.git/` exists at vault root):
- After ingest: commit with message `wiki: ingest <source-name> (N pages)`
- After lint with fixes (only if user explicitly asked to fix): commit with `wiki: lint fixes`
- Never push automatically. User controls remote.

If the vault is not a git repo: skip silently. No warnings.

## Working directory rule

All wiki operations run with the vault directory as cwd. If the cwd doesn't look like a vault (missing `schema/`, `sources/`, `wiki/`), abort and ask the user to `cd` to the vault.

## Output discipline

After any action, print a structured summary:
- What was touched (list of pages with wikilinks)
- What was created vs updated
- What was skipped and why
- Next suggested action (if any)

Never silently succeed with no output.
```

- [ ] **Step 2: 검증 - SKILL.md 형식 확인**

```bash
cd ~/Projects/llm-wiki
head -5 skills/llm-wiki/SKILL.md
```

기대: frontmatter `name:`, `description:` 존재.

- [ ] **Step 3: Commit**

```bash
git add skills/llm-wiki/SKILL.md
git commit -m "feat: llm-wiki skill with operating principles"
```

---

## Task 4: `/wiki-init` 명령 + vault 템플릿

플러그인의 부트스트랩 명령. cwd를 vault로 초기화.

**Files:**
- Create: `~/Projects/llm-wiki/commands/wiki-init.md`
- Create: `~/Projects/llm-wiki/templates/README.template.md`
- Create: `~/Projects/llm-wiki/templates/gitignore.template`

- [ ] **Step 1: `templates/README.template.md` 작성**

```markdown
# {{VAULT_NAME}}

A personal LLM-maintained wiki built with [llm-wiki](https://github.com/<you>/llm-wiki).

## Layout

- `sources/` — immutable originals (claude sessions, manual notes, articles)
- `wiki/` — synthesized pages by namespace
- `schema/` — the rules the LLM follows (your file, your edits)
- `lint-reports/` — periodic health reports

## Common commands

- `/wiki-ingest <source>` — synthesize a source into the wiki
- `/wiki-ask <question>` — query the wiki
- `/wiki-lint` — run the 8 health checks

## Git (optional)

\`\`\`bash
git init
git remote add origin <your-private-repo-url>
git add . && git commit -m "init"
git push -u origin main
\`\`\`

The plugin will auto-commit after ingest/lint if `.git/` exists.
```

- [ ] **Step 2: `templates/gitignore.template` 작성**

```
.DS_Store
.obsidian/workspace.json
.obsidian/workspace-mobile.json
*.swp
.trash/
```

- [ ] **Step 3: `commands/wiki-init.md` 작성**

````markdown
---
description: Bootstrap the current directory as an llm-wiki vault.
---

# /wiki-init

Use the `llm-wiki` skill from this plugin first (load operating principles).

## Pre-flight

1. Check cwd is empty or only contains `.git/`, `.obsidian/`, `.claude/`.
   - If it contains other files, abort: "Directory is not empty. Choose an empty directory."
2. Confirm with the user: "Initialize current directory `<cwd>` as a vault?"

## Bootstrap

Create directory structure:

```
sources/claude-sessions/
sources/manual/
sources/conversations/
wiki/personal/
wiki/work/
wiki/tech/
wiki/projects/
wiki/people/
lint-reports/
schema/   # populated by copy below
```

Copy plugin templates into vault:

- Copy every file in `<plugin>/templates/schema/` → `<vault>/schema/`
- Copy `<plugin>/templates/README.template.md` → `<vault>/README.md`, replacing `{{VAULT_NAME}}` with the cwd basename.
- Copy `<plugin>/templates/gitignore.template` → `<vault>/.gitignore`

Add `_index.md` stubs:

- `<vault>/sources/_index.md` — heading only: `# Sources`
- `<vault>/wiki/_index.md` — heading only: `# Wiki`

## Post-bootstrap output

Print:

```
Vault initialized at <cwd>.

Next steps (optional, recommended):

  1. Customize schema:
     - Edit schema/namespaces.md to fill in your cwd→namespace mapping.

  2. Enable Claude session auto-capture:
     - Run: bash <plugin>/scripts/install-hook.sh
     - This will guide you to register the SessionEnd hook globally.

  3. Track with git:
     - git init && git add . && git commit -m "init"
     - Add a private remote and push.

  4. Start using:
     - /wiki-ingest sources/manual/<some-note>.md
     - /wiki-ask "<your question>"
     - /wiki-lint
```

## Do not

- Initialize a non-empty directory.
- Touch existing `.git/`, `.obsidian/`, `.claude/`.
- Run `git init` automatically — user's choice.
- Register the SessionEnd hook automatically — user's choice.
````

- [ ] **Step 4: 검증 - 명령 파일 frontmatter 확인**

```bash
cd ~/Projects/llm-wiki
head -3 commands/wiki-init.md
```

기대: `---` 시작, `description:` 줄 존재.

- [ ] **Step 5: 통합 검증 - 임시 디렉터리에서 init 시뮬레이션**

수동 검증 (이 시점엔 플러그인이 아직 install되지 않았으므로 명령이 의도한 동작을 사용자가 마크다운으로 읽고 mentally 검증):

```bash
mkdir /tmp/wiki-init-test && cd /tmp/wiki-init-test
# 사람이 명령 마크다운대로 수동 실행:
mkdir -p sources/{claude-sessions,manual,conversations}
mkdir -p wiki/{personal,work,tech,projects,people}
mkdir -p lint-reports
cp -r ~/Projects/llm-wiki/templates/schema ./schema
sed "s/{{VAULT_NAME}}/wiki-init-test/" ~/Projects/llm-wiki/templates/README.template.md > README.md
cp ~/Projects/llm-wiki/templates/gitignore.template .gitignore
echo "# Sources" > sources/_index.md
echo "# Wiki" > wiki/_index.md

# 검증
find . -type d | sort
ls schema/
python ~/Projects/llm-wiki/scripts/validate-schema.py schema/
echo "exit code: $?"

# 정리
rm -rf /tmp/wiki-init-test
```

기대:
- 모든 디렉터리 존재
- `schema/`에 6 파일
- validate-schema.py 종료 코드 0

- [ ] **Step 6: Commit**

```bash
cd ~/Projects/llm-wiki
git add commands/wiki-init.md templates/README.template.md templates/gitignore.template
git commit -m "feat: /wiki-init command + vault templates"
```

---

## Task 5: SessionEnd 훅 + 설치 가이드

모든 Claude Code 세션 transcript를 vault `sources/claude-sessions/`에 자동 저장. LLM 호출 0, 셸 스크립트만.

**Files:**
- Create: `~/Projects/llm-wiki/hooks/session-end-capture.sh`
- Create: `~/Projects/llm-wiki/scripts/install-hook.sh`
- Create: `~/Projects/llm-wiki/tests/test_session_end_capture.bats`

- [ ] **Step 1: 실패하는 bats 테스트 먼저 작성**

`tests/test_session_end_capture.bats`:

```bash
#!/usr/bin/env bats

setup() {
    export TEST_DIR="$(mktemp -d)"
    export VAULT_DIR="$TEST_DIR/vault"
    mkdir -p "$VAULT_DIR/sources/claude-sessions"
    mkdir -p "$VAULT_DIR/schema"
    # Minimal namespaces.md
    cat > "$VAULT_DIR/schema/namespaces.md" <<'EOF'
cwd_to_namespace:
  "/test/projects/**": tech
  default: personal
EOF
    export LLM_WIKI_VAULT="$VAULT_DIR"
    HOOK="$BATS_TEST_DIRNAME/../hooks/session-end-capture.sh"
}

teardown() {
    rm -rf "$TEST_DIR"
}

@test "hook writes source file with required frontmatter" {
    INPUT=$(cat <<'EOF'
{
  "session_id": "test-session-123",
  "cwd": "/test/projects/foo",
  "transcript": "user: hi\nassistant: hello"
}
EOF
)
    echo "$INPUT" | "$HOOK"
    [ -f "$VAULT_DIR/sources/claude-sessions/"*.md ]
    FILE=$(ls "$VAULT_DIR/sources/claude-sessions/"*.md | head -1)
    grep -q "type: source" "$FILE"
    grep -q "source_type: claude_session" "$FILE"
    grep -q "processed: false" "$FILE"
    grep -q "namespace: tech" "$FILE"
    grep -q "session_id: test-session-123" "$FILE"
}

@test "hook uses default namespace when cwd matches no pattern" {
    INPUT=$(cat <<'EOF'
{
  "session_id": "test-session-456",
  "cwd": "/random/path",
  "transcript": "ls"
}
EOF
)
    echo "$INPUT" | "$HOOK"
    FILE=$(ls "$VAULT_DIR/sources/claude-sessions/"*.md | head -1)
    grep -q "namespace: personal" "$FILE"
}

@test "hook does nothing when LLM_WIKI_VAULT is unset" {
    unset LLM_WIKI_VAULT
    INPUT='{"session_id":"x","cwd":"/x","transcript":"x"}'
    run bash -c "echo '$INPUT' | '$HOOK'"
    [ "$status" -eq 0 ]
    [ ! -f "$VAULT_DIR/sources/claude-sessions/"*.md ] || \
        ! ls "$VAULT_DIR/sources/claude-sessions/"*.md 2>/dev/null
}
```

- [ ] **Step 2: 테스트 실행하여 실패 확인**

```bash
cd ~/Projects/llm-wiki
# bats가 없으면: brew install bats-core
bats tests/test_session_end_capture.bats
```

기대: FAIL — hook 스크립트 없음.

- [ ] **Step 3: `hooks/session-end-capture.sh` 작성**

```bash
#!/usr/bin/env bash
# SessionEnd hook for llm-wiki.
# Reads JSON from stdin: { session_id, cwd, transcript }
# Writes a source markdown file into $LLM_WIKI_VAULT/sources/claude-sessions/.

set -euo pipefail

# Hard-skip if vault not configured
if [ -z "${LLM_WIKI_VAULT:-}" ]; then
    exit 0
fi

VAULT="$LLM_WIKI_VAULT"
SESSIONS_DIR="$VAULT/sources/claude-sessions"
SCHEMA_NS="$VAULT/schema/namespaces.md"

if [ ! -d "$SESSIONS_DIR" ]; then
    # Vault not initialized yet, skip silently
    exit 0
fi

# Read stdin into a tempfile (handle binary safety)
INPUT=$(cat)

# Extract fields with python (more portable than jq across machines)
python3 - <<PYEOF
import json, os, re, sys, datetime, pathlib, fnmatch

data = json.loads('''$INPUT''')
session_id = data.get("session_id", "unknown")
cwd = data.get("cwd", "")
transcript = data.get("transcript", "")

# Skip trivial sessions (no transcript or < 50 chars)
if not transcript or len(transcript.strip()) < 50:
    sys.exit(0)

# Determine namespace from schema/namespaces.md cwd_to_namespace block
namespaces_md = pathlib.Path(os.environ["SCHEMA_NS"]) if os.environ.get("SCHEMA_NS") else None
namespace = "personal"
try:
    text = pathlib.Path("$SCHEMA_NS").read_text()
    in_block = False
    for line in text.splitlines():
        stripped = line.strip()
        if stripped.startswith("cwd_to_namespace:"):
            in_block = True
            continue
        if in_block:
            if not line.startswith(" ") and not line.startswith("\t") and stripped:
                break
            if stripped.startswith("#") or not stripped:
                continue
            m = re.match(r'"([^"]+)"\s*:\s*(\w+)', stripped)
            if m:
                pattern, ns = m.group(1), m.group(2)
                glob = pattern.replace("**", "*")
                if fnmatch.fnmatch(cwd, glob) or cwd.startswith(pattern.split("**")[0]):
                    namespace = ns
                    break
            m2 = re.match(r"default\s*:\s*(\w+)", stripped)
            if m2:
                namespace = namespace if namespace != "personal" else m2.group(1)
except FileNotFoundError:
    pass

# Build slug from cwd basename + session id prefix
cwd_base = pathlib.Path(cwd).name or "session"
slug = re.sub(r'[^a-zA-Z0-9가-힣\-]', '-', cwd_base)[:40].strip("-").lower() or "session"
date = datetime.date.today().isoformat()
filename = f"{date}-{slug}-{session_id[:8]}.md"

now_iso = datetime.datetime.now().isoformat(timespec="seconds")

content = f"""---
type: source
source_type: claude_session
namespace: {namespace}
created: {date}
updated: {date}
ingested_at: {now_iso}
processed: false
session_id: {session_id}
cwd: {cwd}
---

# Claude session: {cwd_base}

{transcript}
"""

out = pathlib.Path("$SESSIONS_DIR") / filename
out.write_text(content)
PYEOF
```

- [ ] **Step 4: 실행권한 + 테스트 실행하여 통과 확인**

```bash
cd ~/Projects/llm-wiki
chmod +x hooks/session-end-capture.sh
export SCHEMA_NS=""  # tests set this internally
bats tests/test_session_end_capture.bats
```

기대: 3 tests PASS.

- [ ] **Step 5: `scripts/install-hook.sh` 작성 (가이드 출력)**

```bash
#!/usr/bin/env bash
# Guide the user to register the SessionEnd hook globally.
# This script does NOT modify ~/.claude/settings.json automatically.

set -euo pipefail

PLUGIN_DIR="$(cd "$(dirname "$0")/.." && pwd)"
HOOK_PATH="$PLUGIN_DIR/hooks/session-end-capture.sh"

cat <<EOF
=== llm-wiki: SessionEnd hook installation ===

This hook captures every Claude Code session into your vault's
sources/claude-sessions/ directory.

To enable, add the following to ~/.claude/settings.json under "hooks":

{
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "$HOOK_PATH"
          }
        ]
      }
    ]
  }
}

Also set this environment variable in your shell rc (e.g., ~/.zshrc):

  export LLM_WIKI_VAULT="\$HOME/Vaults/<your-vault-name>"

If LLM_WIKI_VAULT is unset, the hook does nothing — safe default.

Verify after adding:
  echo '{"session_id":"test","cwd":"\$(pwd)","transcript":"test data with enough length to pass the trivial-session filter."}' | bash "$HOOK_PATH"
  ls "\$LLM_WIKI_VAULT/sources/claude-sessions/"

EOF
```

- [ ] **Step 6: 실행권한 + 출력 확인**

```bash
cd ~/Projects/llm-wiki
chmod +x scripts/install-hook.sh
bash scripts/install-hook.sh
```

기대: 위 가이드 텍스트가 출력되고 종료 코드 0.

- [ ] **Step 7: Commit**

```bash
git add hooks/session-end-capture.sh scripts/install-hook.sh tests/test_session_end_capture.bats
git commit -m "feat: SessionEnd capture hook + global install guide"
```

---

## Task 6: `/wiki-ingest` 명령

source → wiki 합성. spec §5.3.

**Files:**
- Create: `~/Projects/llm-wiki/commands/wiki-ingest.md`
- Create: `~/Projects/llm-wiki/tests/fixtures/sample-source.md`

- [ ] **Step 1: `tests/fixtures/sample-source.md` 작성 (통합 테스트용 픽스처)**

```markdown
---
type: source
source_type: manual
namespace: tech
created: 2026-05-18
updated: 2026-05-18
ingested_at: 2026-05-18T10:00:00
processed: false
---

# Prompt caching basics

Prompt caching reduces the per-request cost of repeated context.
Anthropic exposes it via `cache_control: { type: "ephemeral" }` on a message block.
Cache TTL is 5 minutes; misses recompute fully.

Used with Claude Code's system prompt and stable tool definitions.
```

- [ ] **Step 2: `commands/wiki-ingest.md` 작성**

````markdown
---
description: Synthesize a source (or batch of recent sources) into the wiki.
---

# /wiki-ingest [source-path | --recent <duration>]

Use the `llm-wiki` skill from this plugin first (load schema and invariants).

## Argument parsing

- No args → ingest all `sources/**/*.md` with `processed: false`.
- A single file path → ingest just that file.
- `--recent 1d` / `--recent 7d` → ingest unprocessed sources whose `ingested_at` is within the duration.

## Per-source procedure (follow `schema/ingest-rules.md`)

For each target source:

1. **Read source** — full content + frontmatter. Note `namespace`.

2. **Identify candidates** — search the wiki:
   - Grep wiki/<namespace>/ for keywords from source title + body (top 20 terms by TF).
   - Check aliases in topic/entity frontmatter.
   - Read top 5-10 candidate pages.

3. **Classify what's needed**:
   - Existing topic to update? Which?
   - New topic to create?
   - Note that's too raw to synthesize?
   - Question raised by source?
   - Entity (person/tool/company) first mention?

4. **Apply changes**:
   - **Update existing**: append new section or paragraph. Add `sources: [[<this-source>]]` to frontmatter (deduplicated). Update `updated:`.
   - **Create new**: full frontmatter per `page-types.md`. Filename per type. Non-empty `sources:` with this source.
   - **Relations**: when this change connects two pages meaningfully, add `related:` (or other appropriate relation) on both sides.
   - **Contradictions**: if new info contradicts existing claim, add `contradicts:` on both sides AND leave the older claim with a body marker (do not delete).

5. **Mark source processed**:
   - Update source frontmatter: `processed: true`, `updated: <today>`.

6. **Commit (if git)**: see `llm-wiki` skill section on commit behavior.

## Report

After all sources processed, print:

```
Ingested N sources, touched M pages.

Created:
  - [[<page>]] (<type>, <namespace>)
  ...

Updated:
  - [[<page>]]: <one-line summary of change>
  ...

Skipped:
  - <source>: <reason>
  ...

Questions raised:
  - [[q-<slug>]]
```

## Failure modes

- Source file not found → error, abort.
- Source frontmatter invalid (missing `type` or `namespace`) → error, ask user to fix source.
- Schema missing/invalid → ask user to run `/wiki-init` or fix schema.

## Forbidden

- Never modify `sources/` content (only `processed:` and `updated:` flags).
- Never modify `schema/`.
- Never remove existing `sources:` or relation entries — only append.
````

- [ ] **Step 3: 검증 - 명령 마크다운 frontmatter 확인**

```bash
head -3 ~/Projects/llm-wiki/commands/wiki-ingest.md
```

기대: frontmatter 존재.

- [ ] **Step 4: 통합 검증 - mental walkthrough**

`tests/fixtures/sample-source.md`를 가상의 빈 vault에 두고, 명령 마크다운 instructions를 머릿속으로 따라가며:
- 빈 vault → topic 새 페이지 `prompt-caching.md` 생성됨 (namespace: tech)
- frontmatter 완전 (`type: topic`, `namespace: tech`, `sources: [[2026-05-18-...-prompt-caching]]` 등)
- 소스 `processed: true` 마킹

문서 흐름이 위 결과를 산출하기에 충분히 명확한지 검토. 모호하면 명령 마크다운에 절차를 더 적는다.

- [ ] **Step 5: Commit**

```bash
git add commands/wiki-ingest.md tests/fixtures/sample-source.md
git commit -m "feat: /wiki-ingest command + sample source fixture"
```

---

## Task 7: `/wiki-ask` 명령

위키 컨텍스트 기반 질의응답. spec §5.4.

**Files:**
- Create: `~/Projects/llm-wiki/commands/wiki-ask.md`

- [ ] **Step 1: `commands/wiki-ask.md` 작성**

````markdown
---
description: Ask a question, answered from the wiki with citations.
---

# /wiki-ask <question>

Use the `llm-wiki` skill from this plugin first.

## Procedure

1. **Search** — locate relevant pages:
   - Grep keywords across `wiki/`.
   - Match aliases in topic/entity frontmatter.
   - Prefer `topic` and `entity` pages over `note` and `source`.

2. **Load** — read top 5-10 candidate pages (full content + frontmatter).

3. **Answer** — generate the response with these constraints:
   - Cite every non-trivial claim with a wikilink `[[<page>]]`.
   - If the wiki has no info, say so explicitly. Do NOT hallucinate.
   - If pages contradict each other, mention the conflict and cite both.
   - Distinguish "wiki says" from "I infer" — only the former gets wikilinks.

4. **Offer to save** — if the answer involved synthesizing new insight:
   - Ask: "This answer combined info from N pages into something new. Save as `note` page?"
   - On user yes: create a `note` page in the appropriate namespace, with `sources:` pointing to the queried pages and `topics:` linking to relevant topics.
   - On no/silence: do nothing.

## Report format

```
<the answer in prose, with [[wikilinks]] inline as citations>

---
Sources consulted:
  - [[page-1]]
  - [[page-2]]

Coverage:
  - <if any aspect of question wasn't in wiki, list here>
```

## Forbidden

- Inventing facts not present in the wiki.
- Auto-saving the answer as a new page without explicit user approval.
- Modifying source pages.
````

- [ ] **Step 2: 검증 - 명령 마크다운 head 확인**

```bash
head -3 ~/Projects/llm-wiki/commands/wiki-ask.md
```

기대: frontmatter 존재.

- [ ] **Step 3: Commit**

```bash
git add commands/wiki-ask.md
git commit -m "feat: /wiki-ask command with citation discipline"
```

---

## Task 8: `/wiki-lint` 명령

8항목 위키 점검. spec §5.5.

**Files:**
- Create: `~/Projects/llm-wiki/commands/wiki-lint.md`

- [ ] **Step 1: `commands/wiki-lint.md` 작성**

````markdown
---
description: Run 8 health checks on the wiki. Findings-only, no auto-fixes.
---

# /wiki-lint

Use the `llm-wiki` skill from this plugin first.
Follow `schema/lint-rules.md` precisely.

## Checks (in order)

For each, gather findings. Do NOT fix anything.

### 1. Unprocessed source backlog
- `find sources/ -name "*.md"` → for each, check frontmatter `processed: false`.
- Count total. Sort by `ingested_at` ascending. Report oldest 10.

### 2. Duplicate candidates
- For each pair of `topic` pages in same namespace:
  - **High confidence**: identical title OR alias overlap (intersect of aliases sets non-empty).
  - **Low confidence**: cosine TF-IDF similarity of body > 0.7.
- For `entity`, same logic.
- Report pairs.

### 3. Orphan pages
- For each non-source page: count inbound refs (any `[[X]]` in another page's body OR frontmatter relation pointing to it).
- 0 inbound → orphan.
- Report list.

### 4. Broken wikilinks
- For every `[[X]]` (body + frontmatter relations), check that `X.md` exists in `wiki/**` or `sources/**`.
- Missing → broken.
- Report `<source-page>`: `[[broken-target]]`.

### 5. Contradiction markers
- Pages with non-empty `contradicts:` frontmatter.
- Report as pairs.

### 6. Stale open questions
- `wiki/**/q-*.md` with `status: open` and `updated:` more than 30 days ago.
- Report list with age.

### 7. Frontmatter violations
- Missing required fields: `type`, `namespace`, `created`, `updated`.
- Invalid `type` (not in {topic, entity, note, source, question}).
- Invalid `namespace` (not in 5 defined).
- Type-specific violations per `page-types.md`.

### 8. Missing sources
- Any non-`source` page with empty/missing `sources:` frontmatter.
- Sources are exempt.

## Output

Write report to `lint-reports/<YYYY-MM-DD>-lint.md`:

```markdown
---
type: lint-report
created: <today>
---

# Lint Report <YYYY-MM-DD>

## Summary

| Check | Count |
|---|---|
| Unprocessed sources | N |
| Duplicate candidates (high conf) | N |
| Duplicate candidates (low conf) | N |
| Orphan pages | N |
| Broken wikilinks | N |
| Contradiction pairs | N |
| Stale open questions | N |
| Frontmatter violations | N |
| Missing-sources pages | N |

## Unprocessed source backlog
- [[<source-1>]] (ingested 2026-05-10)
- ...

## Duplicate candidates (high confidence)
- [[topic-a]] ↔ [[topic-b]] (alias overlap: "X")
- ...

<...remaining sections per check...>
```

After writing, print path to the report and a one-line summary count.

## Forbidden

- Auto-fixing anything.
- Modifying any wiki page (except writing the new lint report).
- Modifying sources.
````

- [ ] **Step 2: 검증**

```bash
head -3 ~/Projects/llm-wiki/commands/wiki-lint.md
```

기대: frontmatter 존재.

- [ ] **Step 3: Commit**

```bash
git add commands/wiki-lint.md
git commit -m "feat: /wiki-lint command with 8 checks (findings-only)"
```

---

## Task 9: `/wiki-upgrade-schema` 명령

플러그인 schema 템플릿 업데이트 시 vault schema에 선택적 머지. spec §5.6.

**Files:**
- Create: `~/Projects/llm-wiki/commands/wiki-upgrade-schema.md`

- [ ] **Step 1: `commands/wiki-upgrade-schema.md` 작성**

````markdown
---
description: Compare plugin's schema templates with vault's schema and selectively merge.
---

# /wiki-upgrade-schema

Use the `llm-wiki` skill from this plugin first.

## Procedure

1. **Locate plugin templates**:
   - Plugin templates live at `<plugin-path>/templates/schema/`.
   - Resolve `<plugin-path>` from the current Claude Code plugin install (the location of this command file's parent's parent).

2. **For each of the 6 schema files** (`README.md`, `page-types.md`, `relations.md`, `namespaces.md`, `ingest-rules.md`, `lint-rules.md`):
   - Run `diff <plugin-template-file> <vault-schema-file>`.
   - If identical, skip silently.
   - If different, present diff to user.

3. **For each diff, ask user**:
   - "Apply this change to your `schema/<file>`?"
   - Options: `yes`, `no`, `show full`, `partial — let me describe what to keep`.
   - User-provided partial instructions are honored verbatim.

4. **Apply approved changes** — modify vault `schema/<file>` exactly as user approved.

5. **Commit (if git)**: `wiki: upgrade schema (<files-changed>)`.

## Output

Print a summary:
```
Schema upgrade complete.
Changed: <files>
Unchanged: <files>
Skipped (no diff): <files>
```

## Forbidden

- Auto-applying any change without user approval per file.
- Overwriting `schema/namespaces.md`'s `cwd_to_namespace` block — that's user-specific. Always ask, never silently merge.
- Modifying any wiki content.
````

- [ ] **Step 2: 검증**

```bash
head -3 ~/Projects/llm-wiki/commands/wiki-upgrade-schema.md
```

기대: frontmatter 존재.

- [ ] **Step 3: Commit**

```bash
git add commands/wiki-upgrade-schema.md
git commit -m "feat: /wiki-upgrade-schema command with per-file user approval"
```

---

## Task 10: 플러그인 README 보강 + 첫 push

플러그인을 한 번 정돈하고 사용자가 GitHub에 push할 수 있는 상태로.

**Files:**
- Modify: `~/Projects/llm-wiki/README.md`

- [ ] **Step 1: `README.md` 보강**

```markdown
# llm-wiki

> Memex realized with LLMs.

A Claude Code plugin that turns your knowledge into a wiki the model maintains.
Inspired by [Karpathy's LLM wiki pattern](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f).

## Three layers (Karpathy)

- **Sources** — immutable originals (Claude sessions, manual notes, articles)
- **Wiki** — synthesized pages by namespace; the LLM keeps them current
- **Schema** — the rules the LLM follows (you own these files)

## Install

\`\`\`
/plugin install <github-url>
\`\`\`

## Bootstrap a vault

\`\`\`bash
mkdir -p ~/Vaults/<your-wiki>
cd ~/Vaults/<your-wiki>
claude
> /wiki-init
\`\`\`

`/wiki-init` creates the directory structure, copies the schema templates, and prints next-step guidance.

## Enable automatic session capture (optional)

\`\`\`bash
bash ~/.claude/plugins/.../llm-wiki/scripts/install-hook.sh
\`\`\`

This guides you through adding a `SessionEnd` hook to `~/.claude/settings.json` and setting `LLM_WIKI_VAULT` in your shell.

## Commands

| Command | What it does |
|---|---|
| `/wiki-init` | Bootstrap current dir as a vault |
| `/wiki-ingest [path|--recent]` | Synthesize sources into the wiki |
| `/wiki-ask <question>` | Answer from the wiki with citations |
| `/wiki-lint` | Run 8 health checks; write report |
| `/wiki-upgrade-schema` | Diff and merge updated schema templates |

## Page types

5 types — `topic`, `entity`, `note`, `source`, `question`. See `templates/schema/page-types.md`.

## Relations

5 typed relations — `related`, `part_of`, `contradicts`, `supersedes`, `derived_from`. See `templates/schema/relations.md`.

## Hard invariants (day-1)

1. Sources are immutable.
2. Every wiki page has non-empty `sources:`.
3. Frontmatter is the source of truth.
4. Schema is user-owned; the plugin never auto-overwrites it.
5. Git auto-commit only if the vault is a git repo.

## License

MIT
```

- [ ] **Step 2: Commit + 사용자 안내**

```bash
cd ~/Projects/llm-wiki
git add README.md
git commit -m "docs: expand README with commands and invariants"
```

사용자에게 안내: "이제 GitHub에 public 레포 만들고 push할 수 있습니다. `git remote add origin <url>` 후 `git push -u origin main`."

---

## Task 11: 사용자 vault 부트스트랩 (실제 가동)

이제 플러그인을 사용자 vault에 적용. 이 시점에 `~/Vaults/wiki/`는 `docs/superpowers/` 디렉터리만 있는 상태 — `/wiki-init`이 거부할 수 있으므로 처리 필요.

**Files:**
- Modify: `~/Vaults/wiki/` 전체 구조

- [ ] **Step 1: 사용자 vault 현재 상태 확인**

```bash
cd ~/Vaults/wiki
ls -la
```

존재 항목: `.claude/`, `docs/`.

- [ ] **Step 2: `/wiki-init`이 거부할 가능성에 대해 사용자와 조율**

`wiki-init.md`의 pre-flight: "디렉터리는 비어있어야 함" (단 `.git`, `.obsidian`, `.claude` 제외). `docs/`는 비-허용 항목이라 init이 abort.

두 옵션:
- **A**: `docs/`를 임시로 이동 후 init, 끝나면 다시 이동
- **B**: `/wiki-init`의 허용 목록에 `docs/`를 추가하는 패치 (이번 사용자의 spec/plan 보관용)

플러그인 일반화 관점에서 **B**가 부적절(`docs/`는 이 사용자 특수 상황). **A** 권장.

```bash
cd ~/Vaults/wiki
mv docs /tmp/wiki-docs-backup
```

- [ ] **Step 3: 플러그인 install 후 `/wiki-init` 실행**

```bash
# Claude Code 안에서:
> /plugin install ~/Projects/llm-wiki   # 또는 github url
> cd ~/Vaults/wiki
> /wiki-init
```

기대: 디렉터리 구조 생성 + schema 6 파일 + README + .gitignore + 안내 출력.

- [ ] **Step 4: `docs/` 복원 + 검증**

```bash
cd ~/Vaults/wiki
mv /tmp/wiki-docs-backup docs
find . -maxdepth 2 -type d | sort
ls schema/
python ~/Projects/llm-wiki/scripts/validate-schema.py schema/
```

기대:
- `sources/`, `wiki/`, `schema/`, `lint-reports/`, `docs/` 모두 존재
- schema에 6 파일
- validate-schema 종료 코드 0

- [ ] **Step 5: 사용자 환경 cwd 매핑 작성**

`schema/namespaces.md`의 `cwd_to_namespace:` 블록을 사용자가 직접 채움. 사용자에게 묻기:
- 일반 프로젝트 디렉터리는?
- 회사 작업 디렉터리가 별도라면 어디?
- 학습/실험 디렉터리?

수집한 패턴으로 namespaces.md 편집.

- [ ] **Step 6: SessionEnd 훅 활성화 (선택)**

```bash
bash ~/Projects/llm-wiki/scripts/install-hook.sh
```

가이드대로 `~/.claude/settings.json` 편집 + `~/.zshrc`에 `export LLM_WIKI_VAULT="$HOME/Vaults/wiki"` 추가.

`source ~/.zshrc` 후 검증:
```bash
echo '{"session_id":"smoke-test","cwd":"'"$(pwd)"'","transcript":"this is a smoke test transcript long enough to pass the trivial-session filter."}' | \
  bash ~/Projects/llm-wiki/hooks/session-end-capture.sh
ls ~/Vaults/wiki/sources/claude-sessions/
```

기대: 파일 하나 생성됨. 내용 확인.

- [ ] **Step 7: git 관리 시작 (선택)**

```bash
cd ~/Vaults/wiki
git init
git add .
git commit -m "init: vault bootstrapped via llm-wiki"
# 사용자가 private remote 생성 후
# git remote add origin <url>
# git push -u origin main
```

- [ ] **Step 8: smoke test — 한 source 합성**

vault 안에 수동 source 하나 두고 ingest 실행:

```bash
cd ~/Vaults/wiki
cat > sources/manual/2026-05-18-llm-wiki-도입.md <<'EOF'
---
type: source
source_type: manual
namespace: personal
created: 2026-05-18
updated: 2026-05-18
ingested_at: 2026-05-18T15:00:00
processed: false
---

# llm-wiki 도입

오늘부터 Karpathy의 LLM wiki 패턴을 따라 개인 위키를 운영한다.
세 계층: sources, wiki, schema.
첫 도구는 Claude Code 플러그인 형태로 구현했다.
EOF

# Claude Code에서:
> /wiki-ingest sources/manual/2026-05-18-llm-wiki-도입.md
```

기대: 
- `wiki/personal/`에 새 topic 페이지 1~3개 생성 (예: `llm-wiki.md`, `karpathy-wiki-pattern.md`)
- source `processed: true`로 마킹됨
- 페이지 frontmatter에 `sources: [[2026-05-18-llm-wiki-도입]]` 포함

검증:
```bash
grep -l "llm-wiki-도입" wiki/personal/*.md
cat sources/manual/2026-05-18-llm-wiki-도입.md | head -10
```

- [ ] **Step 9: smoke test — lint 1회**

```bash
# Claude Code에서:
> /wiki-lint
```

기대: `lint-reports/2026-05-18-lint.md` 생성. Summary가 합리적 (방금 만든 페이지가 orphan일 수 있으나, sources/ 미처리 0건 등).

- [ ] **Step 10: 마지막 정돈**

```bash
cd ~/Vaults/wiki
git add .
git commit -m "wiki: first ingest + lint smoke test"
```

---

## Self-Review (작성 후 점검)

### Spec coverage

- [x] §3 디렉터리 구조 (플러그인 + vault) → Task 1, 4
- [x] §4.1 5종 페이지 타입 → Task 2 (page-types.md)
- [x] §4.2-4.3 frontmatter 스키마 → Task 2
- [x] §4.4 관계 5종 → Task 2 (relations.md)
- [x] §4.5 namespace → Task 2 (namespaces.md)
- [x] §5.1 `/wiki-init` → Task 4
- [x] §5.2 SessionEnd 훅 → Task 5
- [x] §5.3 `/wiki-ingest` → Task 6
- [x] §5.4 `/wiki-ask` → Task 7
- [x] §5.5 `/wiki-lint` → Task 8
- [x] §5.6 `/wiki-upgrade-schema` → Task 9
- [x] §6.1 설치 흐름 → Task 11
- [x] §6.2 git 정책 → Task 3 (skill), Task 11
- [x] §6.3 분리 원칙 → 전체 plan의 file structure
- [x] §7 진화 경로 — 구현 아님 (README 언급으로 충분)
- [x] §8 미해결 사항 — 후속 plan으로 미룸

### Placeholder scan

- "`<plugin>`" placeholder는 `wiki-init.md`, `wiki-upgrade-schema.md` 본문에 명령 instruction의 일부로 의도적 (LLM이 런타임에 resolve). 플랜 step 내에 "TBD" "TODO" 없음.
- README의 `<your-name>`, `<github-url>` 등은 사용자가 채우는 항목으로 의도적 — README가 템플릿 성격이라 적절.

### Type consistency

- 페이지 타입 5종: `topic`, `entity`, `note`, `source`, `question` — 모든 task에서 일관.
- 관계 5종: `related`, `part_of`, `contradicts`, `supersedes`, `derived_from` — 일관.
- Namespace 5종: `personal`, `work`, `tech`, `projects`, `people` — 일관.
- 명령 5개: `wiki-init`, `wiki-ingest`, `wiki-ask`, `wiki-lint`, `wiki-upgrade-schema` — README/skill/명령 파일 일관.
- 환경변수 `LLM_WIKI_VAULT` — hook과 install-hook.sh 일관.

이상.
